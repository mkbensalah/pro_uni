# React Web App (Real Estate)

This is the code for the web app developed using Express && React framework.

## Quick Start

```bash
# locate the server workspace
cd server

# Install dependencies
npm install

# Serve on localhost:5000
npm run server

# Build for production
npm run build
```
